# Development profile configuration
# Enable debug logging for our application
logging.level.RedSource=DEBUG
logging.level.org.springframework.data.mongodb=DEBUG

# MongoDB Configuration - Local development
# Uncomment and update if you want to use a local MongoDB instance
# spring.data.mongodb.uri=mongodb://localhost:27017/bloodbank

# Disable CSRF for development
spring.mvc.cors.allowed-origins=*

# Enable detailed error messages
server.error.include-message=always
server.error.include-binding-errors=always

# Test data configuration
test.hospital.id=68cc485c1a3a72674d918fd7

# Show SQL queries (if using JPA)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Disable some production security features for development
management.endpoints.web.exposure.include=*
management.endpoint.health.show-details=always
